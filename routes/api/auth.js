const express = require('express');
const router = express.Router();

//@route   GET api/auth
//@desc     
//@access  Public
router.get('/', (req, res) => {
    try {
        const user = User.find( )
        
    } catch (error) {
        
    }
})

module.exports = router; 