import React, { Fragment } from 'react';
import './App.css';

//Functions (arrow) rather than Classes to allow use of hooks 
const App = () => 
    //Fragments are like ghost elements that do not show up in the DOM 
    <Fragment>
        <h1>App</h1>
    </Fragment>

export default App;
